export class bookModel {
    isbn
    title
    cover
    constructor(book) {
        this.isbn = book.isbn
        this.title = book.title
        this.cover = book.cover
    }
}

export class bookInformationModel {
    isbn
    title
    cover
    constructor(book) {
        this.isbn = book.isbn
        this.title = book.title
        this.cover = book.cover
    }
}